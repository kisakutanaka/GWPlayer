# Step.md

Plan.mdの内容を、小さく検証可能なステップに分解し、順番に実装を進めるためのロードマップ。各ステップは「動くものが一つ増える」粒度に区切り、都度動作確認しながら進める。

---

## 対象イベント（MVP）

最初はGW150914（人類初の重力波直接検出、ブラックホール連星合体、教育資料が豊富）1件に絞って、全機能を縦方向に通す。複数イベント対応は後回し。

## 技術スタック（提案）

- Vite + TypeScript（フレームワークなし、Vanilla DOM操作。UIのシンプルさを優先）
- Web Audio API（音声再生・フィルタリング）
- Canvas 2D（グラフ描画・アニメーション。軽量ライブラリより自前実装を優先しモバイル性能を確保）
- データ前処理はNode.jsスクリプト（ビルド時のみ実行。ランタイムはブラウザのみで完結させる）
- GitHub Pages + GitHub Actions（デプロイ）

---

## Phase 0: 環境構築

- [x] 0-1 Vite + TypeScriptでプロジェクトを初期化する
- [x] 0-2 GitHub Pages向けのbase path設定と、mainブランチへのpushで自動デプロイするGitHub Actionsを構築し、Hello Worldが公開できることを確認する（以降、常に最新状態が実機で確認できるようにする）
- [x] 0-3 ESLint/Prettierなど最低限のコード品質ツールを導入する（過剰な設定はしない）
- [ ] 0-4 iPhone実機でHello Worldにアクセスし、表示確認手順を確立する（手順は下記「実機確認手順」参照。ユーザーによる実機操作が必要）

## Phase 1: データ準備（GW150914）

- [x] 1-1 GWOSCからGW150914のstrainデータをダウンロードするスクリプトを作成する（`scripts/prepare-gw150914-data.mjs`、H1検出器・4096Hz・32秒・txt.gz形式を使用）
- [x] 1-2 Float32配列のバイナリに変換するスクリプトを作成する（ダウンサンプリングは不要と判断。4096Hz・32秒=131072サンプルはブラウザで扱うのに十分小さいため）
- [x] 1-3 変換済みデータを`public/data/gw150914/`に配置し、fetchで読み込めることを確認する（`src/data/gw150914.ts`、vite previewでHTTP応答とサイズを確認済み）

## Phase 2: 無加工データの再生

- [x] 2-1 Float32配列からAudioBufferを生成し再生するだけのシンプルな機能を作る（`src/audio/strain-player.ts`。物理量のままでは無音のためピーク振幅基準で音量スケーリングのみ実施）
- [x] 2-2 再生/一時停止ボタンのUIを作る
- [x] 2-3 再生位置に応じたシークバーを表示する（ドラッグ中は自動更新を止め、離すとseekする）
- [x] 2-4 デプロイ後、iPhone実機で音声再生（タップ操作・音量・自動再生制限の挙動）を確認する

## Phase 3: 無加工波形グラフ

- [x] 3-1 Canvasで振幅波形を描画する（`src/graph/waveform.ts`。ピクセル単位のmin/max包絡線を描画）
- [x] 3-2 再生位置に連動する現在位置インジケータを表示する（波形は静止画として1回だけ描画し、プレイヘッドのみ別Canvasに毎フレーム再描画して負荷を抑制）

## Phase 4: 周波数成分（FFT/PSD）

- [x] 4-1 FFTライブラリを選定する、または自前実装（Cooley-Tukey）する（`src/dsp/fft.ts`。既知の正弦波でピーク位置を検証済み）
- [x] 4-2 Welch法などでPSDを計算しグラフ表示する（`src/dsp/psd.ts`, `src/graph/spectrum.ts`。ASD=√PSDをlog-logで描画。パーセバルの定理で正規化を検証済み）
- [x] 4-3 PSDとは何かを説明する教育目的のテキストを添える

## Phase 5: ホワイトニング

- [x] 5-1 PSDを使ってホワイトニング処理を実装する（`src/dsp/whiten.ts`。FFT→PSDで割る→IFFT。データ端の不連続によるリンギングをTukey窓で抑制。ASD比930倍→8.7倍まで平坦化されることを検証済み）
- [x] 5-2 ホワイトニング後の波形をグラフ表示する（既存の波形描画・再生UIを`src/ui/player-section.ts`に共通化して再利用。ホワイトニング後のPSDグラフも追加し、無加工との山の違いを視覚的に比較できるようにした）
- [x] 5-3 ホワイトニング後の音声をAudioBuffer化して再生できるようにする

## Phase 6: バンドパス

- [x] 6-1 Web Audio APIのBiquadFilterNode、または自前IIRでバンドパスを実装する（`src/dsp/bandpass.ts`。ハイパス+ローパスをOfflineAudioContextでレンダリング。ホワイトニング後のデータに35〜350Hz[GW150914の信号帯域]を適用）
- [x] 6-2 バンドパス後の波形をグラフ表示する（PSDグラフでも35〜350Hzのプラトーと急峻な減衰を確認済み）
- [x] 6-3 バンドパス後の音声を再生できるようにする
- [x] 6-4 デプロイ後、iPhone実機で3種類の音声とグラフ描画のパフォーマンス（処理落ち・発熱等）を確認する

## Phase 7: 3種類の音声切り替えUI

> タブ切り替えではなく、以下の構成に決定した:
> 最上部に「ヒーローセクション」を置き、アニメーション・チャープ曲線・
> バンドパス後の波形を1つの共有タイムライン(1つの再生ボタン)で同期させる。
> その下にスクロールすると、無加工→PSD→ホワイトニング→バンドパスの
> 4ステップが順番に並び、それぞれ個別のプレイヤーで確認できる。
> タブによる切り替えより、体験(ヒーロー)と仕組みの理解(ステップ)を
> 分けた方が分かりやすいと判断したため。

- [x] 7-1 無加工/ホワイトニング/バンドパスを行き来できるUIを作る（タブではなくヒーロー+ステップ構成。`src/ui/hero-section.ts`で3つの表示を1つの再生ボタンに統合）
- [x] 7-2 モード切替に応じてグラフと音声が連動して切り替わることを確認する（Playwrightでヒーロー内の波形プレイヘッド・アニメーション・チャープ曲線プレイヘッドが同一時刻で同期することを確認済み）

## Phase 8: ブラックホール合体アニメーション

- [x] 8-1 GW150914のパラメータ（質量比、chirp mass等）から簡易的なインスパイラル運動モデルを実装する（`src/physics/inspiral.ts`。リーディングオーダーのチャープ公式+ケプラーの第三法則。信号がバンド内(35Hz)に入るのが合体0.19秒前になるなど、公表値と整合することを検証済み）
- [x] 8-2 Canvasで2体が近づき合体するアニメーションを実装する（`src/graph/orbit.ts`。Playwrightでt=0/8/15/15.42/15.6sの見た目を確認し、離れた軌道→接近→合体後は単一の天体、という想定通りの挙動を確認済み。斜め視点(縦方向を圧縮した投影+奥行きに応じた透明度)と、軌道位相基準で一定範囲を残す軌跡を追加）
- [x] 8-3 音声の再生位置とアニメーションのタイムラインを同期する（バンドパス後の音声で駆動。`src/ui/playback-controls.ts`に再生/シーク制御を共通化し、波形プレイヘッドと軌道アニメーションの両方から再利用。Phase 7のUI再構成でアニメーション単体のセクションは廃止し、`src/ui/hero-section.ts`としてバンドパス波形と統合した）
- [ ] 8-4 デプロイ後、iPhone実機でアニメーションと音声のズレ・描画負荷を確認する

## Phase 9: UI統合・教育コンテンツ

- [ ] 9-1 教育目標4項目に対応する説明文をUIに配置する
- [ ] 9-2 全体レイアウトを整理し、不要な装飾を削ってシンプルにする
- [ ] 9-3 iPhone実機で操作性を検証する（タップ範囲、フォントサイズ等）

## Phase 10: 仕上げ

- [ ] 10-1 iPhoneでのCanvas描画・FFT計算負荷を最終確認し、必要なら軽量化する
- [ ] 10-2 Plan.mdのユーザー体験1〜3が満たされているか、実機で最終確認する

## Phase 11: 重力波イベントカタログ選択

> ユーザー要望により追加。アプリにはカタログ情報（イベント名・短い紹介文）だけを
> 持たせ、実際のstrainデータはプルダウン選択時にGWOSCから直接ダウンロードする。
> GW150914は起動時に読み込み済み（現行のバンドル方式）のまま、他イベントは
> 選択時に取得する。イベントに複数バージョン（カタログ改訂）がある場合は、
> strainファイルを持つ最新版・パラメータ（質量）を持つ最新版をそれぞれ採用する。

- [x] 11-1 アプリに同梱するカタログデータ（イベントID・短い紹介文）を用意する
      （`src/data/catalog.ts`。GW150914/GW151226/GW170608/GW170817/GW190521の5件を
      GWOSC APIで実在確認済み）
- [x] 11-2 GWOSCの公開APIで、指定イベントの検出器・GPS時刻・質量パラメータ・
      strainファイルURLを解決するモジュールを実装する（`src/data/gwosc-api.ts`。
      strainファイルは実在する最新版、パラメータは存在する最新版をそれぞれ探索）
- [x] 11-3 ブラウザ上でtxt.gzを取得・解凍（DecompressionStream）・パースする
      汎用strainローダーを実装する（`src/data/remote-strain.ts`。GWOSCはCORS全許可
      (`access-control-allow-origin: *`)を確認済み。ヘッダ行から実際のサンプリング
      レート・GPS開始時刻を読み取る）
- [x] 11-4 プルダウンUIを追加し、選択時に上記フローでデータ取得→
      ヒーロー+4ステップ全体を再構築する（`main.ts`の`loadAndRender()`。
      バンドパス帯域はGW150914のみ公表値(35〜350Hz)固定、他イベントは質量から
      動的に見積もる。実装中に発見したバグ2件を修正: (1)再利用するボタン/
      シークバーへイベント切替のたびリスナーが積み重なり古いAudioContextを
      操作してしまう問題→`setupPlaybackControls`にdispose機構を追加、
      (2)切替中に別イベントが選択されると古い非同期処理が後から結果を
      上書きするレースコンディション→世代トークンで防止）
- [x] 11-5 有名イベントには短い補足コメントを付ける（カタログのblurbとして実装、
      プルダウン直下に表示）
- [x] 11-7 教育的に有名なイベントを追加しカタログを拡張する（GW170814/GW190412/
      GW190425/GW190814/GW200115の5件をGWOSC APIでstrainファイル・質量パラメータの
      実在を確認して追加、計10件に。3検出器同時観測・非対称質量・2例目のBNS・
      質量ギャップの謎天体・NSBHという教育的に異なる切り口を選定。あわせて、
      プルダウンを開いた状態でもイベントの特徴が分かるよう、各`<option>`のテキスト
      自体に短い一言(`hint`)を含めた（例:「GW190814 (2019) — 質量ギャップの謎天体」）。
      選択後に表示される`blurb`は従来通りより詳しい説明文として残した）
- [ ] 11-6 デプロイ後、iPhone実機でイベント切り替え動作を確認する

---

## 進め方の原則

- 各ステップは「動くものが増える」単位で区切り、都度動作確認する
- 凝った演出はPlan.mdの設計方針（不要な機能や華美な装飾を追加しない）に反するため後回し、もしくは実施しない
- 技術選定に迷ったら「ブラウザのみで完結」「iPhoneで動く」「教育効果」を優先基準にする

## 実機確認手順

mainにpushするとGitHub Actionsが自動でビルド・デプロイする。実機確認は以下の手順で行う。

1. （初回のみ）GitHubリポジトリの Settings → Pages → Build and deployment → Source を「GitHub Actions」に設定する
2. リポジトリの Actions タブでワークフロー「Deploy to GitHub Pages」が緑チェックで完了していることを確認する
3. iPhoneのSafariで `https://kisakutanaka.github.io/GWPlayer/` を開く
4. 表示・操作を確認する（Step.md中の「実機確認」項目ではここで確認したい内容を都度追記していく）
