# Findings.md

開発中にGWOSCの公開データや物理モデルを検証する過程で得られた、学術的に興味深い発見・確認事項をまとめる。実装の詳細はコード側のコメントを参照。

---

## GW150914の重力波周波数の時間発展（チャープ）

`src/physics/inspiral.ts`で、リーディングオーダー（Newtonian quadrupole近似）のチャープ公式

```
f_gw(τ) = (1/π) · (5 / (256τ))^(3/8) · (G·Mc/c³)^(-5/8)
```

（τ = 合体までの残り秒数、Mcはチャープ質量）を使い、GW150914の質量パラメータ（約36+29太陽質量）から周波数の時間発展を計算したところ、以下の値が得られた。

| τ（合体前） | f_gw |
|---|---|
| 15.4秒（クリップ開始） | 約6.8 Hz |
| 1秒 | 約18.8 Hz |
| 0.19秒 | 35 Hz（バンドパス下限） |
| 0.001秒 | 250 Hz |

この「信号が観測帯域（35Hz）に入ってから合体まで約0.2秒」という結果は、LIGOの発見論文（Abbott et al. 2016）に記載されている「信号は約0.2秒かけて35Hzから250Hzまで駆け上がった」という公表事実とほぼ正確に一致した。簡略化したリーディングオーダー近似でも、この特徴的なタイムスケールを正しく再現できることが確認できた。

## チャープ質量の検算とGWOSC公式値の差

質量36+29太陽質量から式 `Mc = (m1·m2)^(3/5) / (m1+m2)^(1/5)` で計算したチャープ質量は **約28.1太陽質量**。

一方、GWOSC API（`/api/v2/event-versions/GW150914-v4/parameters`）が返す公式パラメータでは `chirp_mass_source` = **27.9太陽質量**（mass_1_source=34.6, mass_2_source=downstream値）となっており、非常に近いが完全には一致しない。これは、よく引用される「36+29太陽質量」がやや丸められた検出器フレームの値である一方、GWOSCの公式パラメータはソースフレーム（赤方偏移補正後）の事後分布中央値であるため。教育用途では気にするレベルの差ではないが、「よく見る数字」と「公式パラメータAPIの数字」が微妙に異なりうる点は実装上留意が必要だった。

## ホワイトニングによるASDの平坦化（定量評価）

`src/dsp/whiten.ts`のホワイトニング処理を適用する前後で、20〜1000Hz帯域におけるASD（振幅スペクトル密度）の最大/最小比を比較した。

- 無加工データ: 約930倍（LIGOノイズカーブの「山」で3桁近く変化）
- ホワイトニング後: 約8.7倍

処理の効果を「山が平らになる」という定性的な説明だけでなく、定量的にも確認できた。

## FFTベースのホワイトニングにおける端の不連続（リンギング）

32秒のデータ全体に1回のFFTをかけてホワイトニングする実装では、データ両端（t=0秒とt=32秒）が信号の途中で唐突に切れているため、逆変換後の波形の端に本来存在しないはずの巨大な振幅（他の部分の数十〜百倍）が現れた。

具体的には、無加工処理では端から離れた領域（±1秒を除く）の振幅が±4程度に収まるのに対し、端の1サンプル目・最終サンプルでは±265を超える値になっていた。これはFFTが暗黙に信号を周期的とみなすことに起因する古典的な現象で、両端をTukey窓（テーパー窓）で滑らかにゼロへ近づけることで解消した。GWOSCの公式チュートリアルでも同様の前処理（tapering）が行われている。

## PSD推定の数値検証（パーセバルの定理）

Welch法によるPSD推定の正規化が正しいかを、既知振幅の正弦波でパーセバルの定理（信号のPSDを全周波数で積分した値は信号の平均二乗値に等しい）を使って検証した。振幅2.0、周波数100Hzの正弦波に対し、PSD積分値と平均二乗値がともに2.0000（有効数字4桁で一致）となり、正規化係数 `1/(fs·Σw²)` および片側スペクトル化（×2）の実装が正しいことを数値的に確認できた。

## GWOSCの公開APIとCORSポリシー

`gwosc.org`のAPI（`/api/v2/...`）およびイベントファイル配信エンドポイント（`/eventapi/json/.../*.txt.gz`）は、レスポンスヘッダに `access-control-allow-origin: *` を含んでおり、**ブラウザから任意のオリジンで直接fetchできる**（CORS全許可）。これにより、GitHub Pagesでホストする本アプリから、サーバーを介さずブラウザ上で直接GWOSCのデータをダウンロードする構成が実現できた。

## GWOSCイベントAPIのバージョニングの癖

1つのイベント（例: GW150914）は複数の「バージョン」を持ち、それぞれ異なるカタログ（例: `O1_O2-Preliminary` → `GWTC-1-confident` → `GWTC-2.1-confident`）に対応する。パラメータ推定（質量など）は版が進むごとに更新されるが、**strainファイル（観測データそのもの）は必ずしも全バージョンに紐づいていない**。例えばGW150914のv4（GWTC-2.1-confident）はパラメータのみでstrainファイルは0件、実際のデータはv1〜v3にのみ存在する。そのため「パラメータは最新版から、strainデータは実際にファイルが存在する最新版から」という2段階の解決ロジックが必要だった。

同様にGW170608は、GWOSCの `detectors` フィールドには `L1` のみが列挙されているにもかかわらず、実際のstrainファイル一覧にはH1のデータも存在した。メタデータのフィールドを鵜呑みにせず、実際のファイル一覧を確認する実装にしている。

## GWOSCのASCII strainファイルの形式

`*.txt.gz`形式は、3行のコメントヘッダ（`#`で始まる。検出器名・サンプリングレート・GPS開始時刻と長さの情報を含む）に続けて、1行1サンプルの浮動小数点数（strain値、単位なしの無次元量、～10⁻²¹オーダー）が並ぶ単純なテキスト形式。gzip圧縮されているが、ブラウザの `DecompressionStream('gzip')`（Safari 16.4以降で利用可）で解凍でき、外部ライブラリなしでパースできる。

## 合体直前の周波数は質量に反比例してスケールする（複数イベントでの検証）

複数の重力波イベント（カタログ機能で選択可能にした5件）について、`estimateMergerFrequency()`（チャープ公式をτ=1msまで外挿）で合体直前の重力波周波数を見積もったところ、以下のように総質量が小さいほど高い周波数まで駆け上がることが確認できた。

| イベント | 質量(太陽質量) | 合体直前(τ=1ms)の推定周波数 |
|---|---|---|
| GW190521（最重量級BBH） | 約98+57 | 約149 Hz |
| GW150914 | 約36+29 | 約250 Hz（公表値と整合） |
| GW151226 | 約14.2+7.5 | 約514 Hz |
| GW170608（最軽量級BBH） | 約10.6+7.8 | 約554 Hz |
| GW170817（中性子星連星） | 約1.5+1.3 | 約1811 Hz |

これは軌道周波数がISCO半径(r∝GM/c²)近傍で総質量Mにほぼ反比例してスケールするという理論的予想と整合する。中性子星連星（GW170817）は総質量が太陽の3倍弱と非常に軽いため、合体直前には4kHzサンプリングのNyquist周波数(2048Hz)近くまで信号が達しうることが分かり、バンドパスフィルタの上限をイベントごとに動的に決める必要があるという実装上の判断の根拠になった。
